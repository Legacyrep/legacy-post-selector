# Personal Claude Code overrides — user-specific permissions etc.
# .claude/settings.json IS committed (project-level hook config).
.claude/settings.local.json
.claude/worktrees/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
